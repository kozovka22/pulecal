sonata_admin:
    title: 'Pulecal Admin'
    
    security:
        handler: sonata.admin.security.handler.role
        role_admin: ROLE_USER
        role_super_admin: ROLE_USER
        information:
            GUEST:    [VIEW, LIST]
            SONATA_ADMIN: [OPERATOR, MASTER, OWNER]

    dashboard:
        blocks:
            - { position: left, type: sonata.admin.block.admin_list }
